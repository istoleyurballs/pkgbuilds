function __do_the_thing() {
  echo "Enabling nix-daemon docker btrfs-scrub ly"
  systemctl enable --now \
    docker.service \
    btrfs-scrub@-.timer \
    btrfs-scrub@mnt-fastass.timer \
    ly@tty2.service
}

post_install() {
  __do_the_thing
}

post_upgrade() {
  __do_the_thing
}
